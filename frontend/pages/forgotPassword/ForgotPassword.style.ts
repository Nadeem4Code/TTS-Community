import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,

    alignItems: 'center',
    padding: 20,
    width: '100%',
  },
  card: {
    width: '100%',
    borderRadius: 5,
  },
  centeredTitle: {
    textAlign: 'center',
    fontSize: 25,
    fontWeight: '800',
    marginTop: 20,
  },
  input: {
    marginBottom: 15,
  },
  submitButton: {
    width: '100%',
    marginTop: 20,
    borderRadius: 5,
    marginBottom: 20,
  },
});
